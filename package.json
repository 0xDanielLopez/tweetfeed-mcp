{
  "name": "tweetfeed-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol (MCP) server for tweetfeed.live — exposes the public IOC feed from Twitter/X as MCP tools for AI agents.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.112.0"
  }
}
